{
  "manifest_version": 3,
  "name": "hacker-news-digest",
  "version": "1.0.0",
  "description": "Custom MCP tool for Hacker News: getTopStories returns the front-page stories.",
  "background": { "service_worker": "background.js" },
  "tools": [
    {
      "name": "getTopStories",
      "description": "Navigates to the Hacker News front page and returns the top stories (rank, title, url, score, comments). Self-contained: it opens HN itself, no prior navigation needed.",
      "inputSchema": {
        "type": "object",
        "properties": { "limit": { "type": "integer", "description": "How many stories (default 10)." } }
      }
    },
    {
      "name": "finish",
      "description": "Call when the task is complete. Pass a short result summary; its value is the final result.",
      "inputSchema": { "type": "object", "properties": { "summary": { "type": "string" } } }
    }
  ]
}
