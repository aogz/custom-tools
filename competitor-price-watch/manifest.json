{
  "manifest_version": 3,
  "name": "competitor-price-watch",
  "version": "1.0.0",
  "description": "Custom MCP tool: readPrices extracts prices and nearby labels from any pricing page.",
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ],
  "tools": [
    {
      "name": "readPrices",
      "description": "Extracts prices (and the nearest plan/product label) from the currently-open page. Pass `url` to target a specific pricing page. Self-contained: if that page isn't open it returns {status:\"navigate_required\", navigateTo}.",
      "inputSchema": {
        "type": "object",
        "properties": { "url": { "type": "string", "description": "Pricing page URL to read." } }
      }
    },
    {
      "name": "finish",
      "description": "Call when the task is complete. Pass a short result summary; its value is the final result.",
      "inputSchema": { "type": "object", "properties": { "summary": { "type": "string" } } }
    }
  ]
}
