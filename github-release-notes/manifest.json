{
  "manifest_version": 3,
  "name": "github-release-notes",
  "version": "1.0.0",
  "description": "Custom MCP tool for GitHub: getLatestRelease returns a repo's latest release notes.",
  "background": { "service_worker": "background.js" },
  "tools": [
    {
      "name": "getLatestRelease",
      "description": "Returns a GitHub repo's latest release (tag, title, published date, notes). Pass `repo` as 'owner/name', e.g. 'vercel/next.js'. Self-contained: navigates to the repo's latest-release page and reads it in one call.",
      "inputSchema": {
        "type": "object",
        "properties": { "repo": { "type": "string", "description": "Repository as 'owner/name', e.g. 'vercel/next.js'." } },
        "required": ["repo"]
      }
    },
    {
      "name": "finish",
      "description": "Call when the task is complete. Pass a short result summary; its value is the final result.",
      "inputSchema": { "type": "object", "properties": { "summary": { "type": "string" } } }
    }
  ]
}
